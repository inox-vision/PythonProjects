## Blender 3D rendering script

Script renders one object per frame using all objects available.
Files are named as 3D objects and placed in directories named after collections.

## Script works under LINUX. Windows users may need to change filepath.
